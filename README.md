# Bank-Management-System-CPP
A robust Console-based Bank Management System developed in C++. Features include client management (Add, Delete, Update, Find) and a transaction module for deposits, withdrawals, and balance tracking, using file handling for data persistence


## 🚀 Features
- **Client Management:** Complete CRUD operations (Create, Read, Update, Delete).
- **Transactions:** Support for Deposits and Withdrawals with real-time balance validation.
- **Data Persistence:** Automatically saves and loads client data from a flat-file database (`Clients.txt`).
- **User Interface:** Clean, menu-driven console interface for easy navigation.

## 🛠️ Built With
- C++
- Standard Template Library (STL) - Vectors, Strings, Algorithms.
- File Streaming (fstream) for data storage.

## 📖 How to Use
1. Clone the repository.
2. Compile the `main.cpp` file using any C++ compiler (G++, MSVC).
3. Run the executable and follow the on-screen menu instructions.
